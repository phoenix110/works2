<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<window xmlns="http://schemas.haulmont.com/cuba/window.xsd"
        caption="msg://browseCaption"
        class="com.cernol.works.web.formula.FormulaBrowse"
        focusComponent="formulasTable"
        lookupComponent="formulasTable"
        messagesPack="com.cernol.works.web.formula">
    <dsContext>
        <groupDatasource id="formulasDs"
                         class="com.cernol.works.entity.Formula"
                         view="_local">
            <query>
                <![CDATA[select e from works$Formula e]]>
            </query>
        </groupDatasource>
    </dsContext>
    <dialogMode height="600"
                width="800"/>
    <layout expand="formulasTable"
            spacing="true">
        <filter id="filter"
                applyTo="formulasTable"
                datasource="formulasDs">
            <properties include=".*"/>
        </filter>
        <groupTable id="formulasTable"
                    width="100%">
            <actions>
                <action id="create"/>
                <action id="edit"/>
                <action id="remove"/>
            </actions>
            <columns>
                <column id="sequenceNo"/>
                <column id="partsPer100"/>
            </columns>
            <rows datasource="formulasDs"/>
            <rowsCount/>
            <buttonsPanel id="buttonsPanel"
                          alwaysVisible="true">
                <button id="createBtn"
                        action="formulasTable.create"/>
                <button id="editBtn"
                        action="formulasTable.edit"/>
                <button id="removeBtn"
                        action="formulasTable.remove"/>
            </buttonsPanel>
        </groupTable>
    </layout>
</window>
